MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=root123
MYSQL_DATABASE=urban_service

# Secret used to sign admin session cookies. Use a long, random value in
# production (e.g. `openssl rand -hex 32`). Must be at least 16 characters.
SESSION_SECRET=dev_change_me_to_a_long_random_secret_value_0123456789

WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token
